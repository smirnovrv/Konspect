
#Область ОписаниеПеременных

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура призакрытии(завершениеработы)

	МассивСтрок = Новый массив;

	Для Каждого СтрокаЭлемент Из таблицастрок Цикл
		МассивСтрок.Добавить(СтрокаЭлемент.Значение_li);
	КонецЦикла;

	структурапараметров = Новый структура;
	структурапараметров.вставить("вид", вид);
	структурапараметров.вставить("тип", тип);
	структураПараметров.Вставить("reversed"	, reversed);
	СтруктураПараметров.Вставить("start"	, start);
	СтруктураПараметров.Вставить("value"	, value);
	СтруктураПараметров.Вставить("Строки"	, МассивСтрок);
	
	ОповеститьОВыборе(СтруктураПараметров);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	 Вид = Перечисления.ВидыСписковHTML.Маркированный;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

#КонецОбласти

#Область ОбработчикиКомандФормы

// Код процедур и функций

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#КонецОбласти
